class Solution {
    public int missingNumber(int[] nums) {
        int a = nums.length;
        int sum = (a*(a+1))/2;
        int t=0;
        for(int i=0;i<a;i++){
            t+=nums[i];
        }
        return(sum-t);
    }
}
